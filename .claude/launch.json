{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "dev",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["scripts/dev-preview.sh"],
      "autoPort": true,
      "cwd": "${workspaceFolder}"
    }
  ]
}
