# raw match data (restricted by football-data.co.uk ToS; regenerable via
# analysis/00_fetch_data.py, frozen by hash in study/data/PROVENANCE.json)
study/data/*.csv
study/data/canonical/
study/outputs/*.log
# NB: the DERIVED tables (study/outputs/*.csv) and figures (study/outputs/fig/*.png)
# ARE tracked — they are the shareable data deposit (see study/outputs/README.md).

# python
__pycache__/
*.pyc
.venv/

# node / astro
site/node_modules/
site/dist/
site/.astro/
node_modules/

# os
.DS_Store

# run stamps (regenerable; the versioned ledger is lineage.json)
study/outputs/_provenance/
outputs/_provenance/
