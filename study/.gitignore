# raw match data (restricted by football-data.co.uk ToS; regenerable, hash-frozen)
data/*.csv
data/canonical/
outputs/*.log
outputs/_provenance/
# derived tables (outputs/*.csv) and figures (outputs/fig/*.png) ARE tracked —
# the shareable data deposit (see outputs/README.md).
__pycache__/
*.pyc
.venv/
.DS_Store

# PDF de submissão (artefato de build; regenerar com docs/paper/build_pdf.sh)
outputs/paper/
