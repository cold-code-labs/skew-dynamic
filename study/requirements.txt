# Analysis-pipeline dependencies — PINNED to the exact version that produced the
# paper's numbers (frozen dataset sha 6905ca53; Python 3.14, see .python-version).
# Bit-for-bit reproduction: use these versions. requirements.lock has the transitive closure.
pandas==3.0.3
numpy==2.5.0
scipy==1.18.0
statsmodels==0.14.6
ruptures==1.1.9
matplotlib==3.11.0
